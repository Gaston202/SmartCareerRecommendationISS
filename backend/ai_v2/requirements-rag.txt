# RAG Dependencies - Install these to get started

# ==============================================================================
# CORE: Required for all RAG functionality
# ==============================================================================

# Local embeddings (FREE alternative to OpenAI)
sentence-transformers>=2.2.0
torch>=2.0.0

# Numerical computations
numpy>=1.23.0

# ==============================================================================
# OPTIONAL: Supabase pgvector backend (for production)
# ==============================================================================

# Supabase client (only needed if USE_SUPABASE_RAG=true)
supabase>=0.0.1

# PostgreSQL driver (required by supabase)
psycopg2-binary>=2.9.0

# ==============================================================================
# OPTIONAL: OpenAI embeddings (if you want to switch providers)
# ==============================================================================

# OpenAI API (only needed if EMBEDDING_PROVIDER=openai)
openai>=1.0.0

# ==============================================================================
# Installation Instructions
# ==============================================================================

# Quick start (in-memory only):
#   pip install -r requirements-rag.txt
#
# With Supabase support:
#   pip install -r requirements-rag.txt [supabase]
#
# With OpenAI support:
#   pip install -r requirements-rag.txt [openai]
#
# With everything:
#   pip install -r requirements-rag.txt [all]
