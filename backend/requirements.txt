# Core FastAPI
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0

# LangGraph Agent
langgraph>=0.0.20
langchain-core>=0.1.0
langchain-community>=0.0.20

# Auth & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0

# Database
supabase>=2.29.0,<3.0.0
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9

# HTTP & Utils
httpx>=0.27.0
redis>=5.0.0
orjson>=3.10.0
python-multipart>=0.0.9
# python-jobspy / pandas / numpy: managed via `make install` (see Makefile).
# jobspy pins numpy==1.26.3 which has no Python 3.14 wheel, so install order matters.
beautifulsoup4>=4.12.0
lxml>=5.3.0
scrapling>=0.4.7
ddgs>=9.14.2

# PDF processing
pdfminer.six>=20221105
PyPDF2>=3.0.0

# Data validation
# Zod is a TypeScript library and should not be in Python requirements.
# Pydantic (already listed above) is the Python equivalent.


# Testing
pytest>=8.3.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0

# Dev tools
ruff>=0.6.0
mypy>=1.11.0
black>=24.0.0

# Production
python-dotenv>=1.0.0

# Optional production dependencies
gunicorn>=21.2.0; platform_system != "Windows"
uvloop>=0.19.0; platform_system != "Windows"

# Ollama AI client (official Python library)
ollama>=0.4.0