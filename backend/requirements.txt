# Backend API Server Requirements
# For running the FastAPI that connects frontend/mobile apps to ai_v2 pipeline

# Web Framework (FastAPI)
fastapi>=0.100
uvicorn>=0.23
python-multipart>=0.0.6

# Legacy Flask support (kept for backward compatibility)
Flask>=3.0
Flask-CORS>=4.0

# Core dependencies
pydantic>=2.0
python-dotenv>=1.0

# Supabase Integration
supabase>=2.0

# LLM API Clients  
openai>=1.0

# PDF Processing (PyMuPDF - better PDF text extraction than pypdf)
PyMuPDF>=1.23.0

# Utilities
requests>=2.31
pyyaml>=6.0
